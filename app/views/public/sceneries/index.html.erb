<!DOCTYPE html>
<html class="fontawesome-i2svg-active fontawesome-i2svg-complete">
<%= stylesheet_link_tag "sceneries", :media => "all" %>
<p id="notice"></p>
  <div class=" px-5 px-sm-0">
    <div class="row">
      <%# ユーザー詳細&投稿ページ %>
      <%= render "public/sceneries/create", scenery: @scenery, user: @user %>
      <div class="col-md-8 offset-md-1">
        <h2>投稿一覧</h2>
        <table class="table table-hover table-inverse">
          <%# 投稿一覧 %>
          <%= render partial: "public/sceneries/index", collection: @sceneries, as: 'sceneries' %>
          <%# ページネーション %>
          <%= render "kaminari/index",  sceneries: @sceneries %>
        </table>
      </div>
    </div>
  </div>
</div>